{
  "copilot_action_name": "LINUX-COLLECT-RUNNING-PROCESSES-COPILOT-ACTION",
  "description": "Collects a comprehensive snapshot of all running processes on the system, including PID, PPID, user, command line, executable path, and SHA256 hash of the executable (if available). Produces NDJSON output containing the full process list for forensic analysis. Writes atomically to the Active Response log with .new fallback if locked.",
  "technology": "Linux",
  "icon": "linux",
  "script_parameters": [
    {
      "name": "ARLog",
      "type": "string",
      "required": false,
      "description": "Active Response NDJSON output path (atomic overwrite; falls back to .new if locked).",
      "default": "/var/ossec/active-response/active-responses.log"
    },
    {
      "name": "LogPath",
      "type": "string",
      "required": false,
      "description": "Path to the rotating human-readable script log.",
      "default": "/tmp/Collect-Running-Processes-script.log"
    },
    {
      "name": "LogMaxKB",
      "type": "integer",
      "required": false,
      "description": "Maximum size of the log file (KB) before rotation.",
      "default": 100
    },
    {
      "name": "LogKeep",
      "type": "integer",
      "required": false,
      "description": "Number of rotated log files to keep.",
      "default": 5
    }
  ],
  "repo_url": "https://raw.githubusercontent.com/socfortress/LINUX-COLLECT-RUNNING-PROCESSES-COPILOT-ACTION/refs/heads/main/",
  "script_name": "Collect-Running-Processes.sh",
  "version": "1.0.0",
  "last_updated": "2025-08-23T00:00:00Z"
}
